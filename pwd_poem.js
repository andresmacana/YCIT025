let frontDoorResponse = 'Stands so high';
console.log(frontDoorResponse[0]);
let letters = frontDoorResponse[0];

function frontDoorPassword(x){
    return 
}
const CODE_WORK = [
    'Compilers intensily bestow',
    'On commencing without ego',
    'Different processes ajar',
    'Exit with zero quick',
  ];
let line = [];
for(let i = 0;i<CODE_WORK.length;i++){
    
    line[i].slice(-1);
    i++;
}
console.log(line);